package dev.hexnowloading.dungeonnowloading.registry;


public final class DNLClientItemModels {
    private DNLClientItemModels() {}

    public static void register() {
        // No extra item models are currently registered. Keep this hook so future items that need
        // standalone (non-blockstate) models have a place to declare them.
    }
}
